/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as web3 from '@solana/web3.js'
import * as beet from '@metaplex-foundation/beet'
import * as beetSolana from '@metaplex-foundation/beet-solana'

/**
 * Arguments used to create {@link State}
 * @category Accounts
 * @category generated
 */
export type StateArgs = {
  protocolAuthority: web3.PublicKey
  fixedFee: beet.bignum
  percentFeeBps: beet.bignum
  isFeeRefund: boolean
  feeBeneficiary: web3.PublicKey
  bump: number
  stopTap: web3.PublicKey
  isWorking: boolean
}

export const stateDiscriminator = [216, 146, 107, 94, 104, 75, 182, 177]
/**
 * Holds the data for the {@link State} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class State implements StateArgs {
  private constructor(
    readonly protocolAuthority: web3.PublicKey,
    readonly fixedFee: beet.bignum,
    readonly percentFeeBps: beet.bignum,
    readonly isFeeRefund: boolean,
    readonly feeBeneficiary: web3.PublicKey,
    readonly bump: number,
    readonly stopTap: web3.PublicKey,
    readonly isWorking: boolean
  ) {}

  /**
   * Creates a {@link State} instance from the provided args.
   */
  static fromArgs(args: StateArgs) {
    return new State(
      args.protocolAuthority,
      args.fixedFee,
      args.percentFeeBps,
      args.isFeeRefund,
      args.feeBeneficiary,
      args.bump,
      args.stopTap,
      args.isWorking
    )
  }

  /**
   * Deserializes the {@link State} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(
    accountInfo: web3.AccountInfo<Buffer>,
    offset = 0
  ): [State, number] {
    return State.deserialize(accountInfo.data, offset)
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link State} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey,
    commitmentOrConfig?: web3.Commitment | web3.GetAccountInfoConfig
  ): Promise<State> {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    )
    if (accountInfo == null) {
      throw new Error(`Unable to find State account at ${address}`)
    }
    return State.fromAccountInfo(accountInfo, 0)[0]
  }

  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(
    programId: web3.PublicKey = new web3.PublicKey(
      'src5qyZHqTqecJV4aY6Cb6zDZLMDzrDKKezs22MPHr4'
    )
  ) {
    return beetSolana.GpaBuilder.fromStruct(programId, stateBeet)
  }

  /**
   * Deserializes the {@link State} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf: Buffer, offset = 0): [State, number] {
    return stateBeet.deserialize(buf, offset)
  }

  /**
   * Serializes the {@link State} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return stateBeet.serialize({
      accountDiscriminator: stateDiscriminator,
      ...this,
    })
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link State}
   */
  static get byteSize() {
    return stateBeet.byteSize
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link State} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    connection: web3.Connection,
    commitment?: web3.Commitment
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(
      State.byteSize,
      commitment
    )
  }

  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link State} data.
   */
  static hasCorrectByteSize(buf: Buffer, offset = 0) {
    return buf.byteLength - offset === State.byteSize
  }

  /**
   * Returns a readable version of {@link State} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      protocolAuthority: this.protocolAuthority.toBase58(),
      fixedFee: (() => {
        const x = <{ toNumber: () => number }>this.fixedFee
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      percentFeeBps: (() => {
        const x = <{ toNumber: () => number }>this.percentFeeBps
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      isFeeRefund: this.isFeeRefund,
      feeBeneficiary: this.feeBeneficiary.toBase58(),
      bump: this.bump,
      stopTap: this.stopTap.toBase58(),
      isWorking: this.isWorking,
    }
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const stateBeet = new beet.BeetStruct<
  State,
  StateArgs & {
    accountDiscriminator: number[] /* size: 8 */
  }
>(
  [
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['protocolAuthority', beetSolana.publicKey],
    ['fixedFee', beet.u64],
    ['percentFeeBps', beet.u64],
    ['isFeeRefund', beet.bool],
    ['feeBeneficiary', beetSolana.publicKey],
    ['bump', beet.u8],
    ['stopTap', beetSolana.publicKey],
    ['isWorking', beet.bool],
  ],
  State.fromArgs,
  'State'
)

/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as beet from '@metaplex-foundation/beet'
import * as web3 from '@solana/web3.js'
import * as beetSolana from '@metaplex-foundation/beet-solana'

/**
 * Arguments used to create {@link AuthorizedNativeSender}
 * @category Accounts
 * @category generated
 */
export type AuthorizedNativeSenderArgs = {
  dstAddress: Uint8Array
  bump: number
}

export const authorizedNativeSenderDiscriminator = [
  218, 129, 184, 31, 240, 103, 170, 112,
]
/**
 * Holds the data for the {@link AuthorizedNativeSender} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class AuthorizedNativeSender implements AuthorizedNativeSenderArgs {
  private constructor(readonly dstAddress: Uint8Array, readonly bump: number) {}

  /**
   * Creates a {@link AuthorizedNativeSender} instance from the provided args.
   */
  static fromArgs(args: AuthorizedNativeSenderArgs) {
    return new AuthorizedNativeSender(args.dstAddress, args.bump)
  }

  /**
   * Deserializes the {@link AuthorizedNativeSender} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(
    accountInfo: web3.AccountInfo<Buffer>,
    offset = 0
  ): [AuthorizedNativeSender, number] {
    return AuthorizedNativeSender.deserialize(accountInfo.data, offset)
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link AuthorizedNativeSender} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey,
    commitmentOrConfig?: web3.Commitment | web3.GetAccountInfoConfig
  ): Promise<AuthorizedNativeSender> {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    )
    if (accountInfo == null) {
      throw new Error(
        `Unable to find AuthorizedNativeSender account at ${address}`
      )
    }
    return AuthorizedNativeSender.fromAccountInfo(accountInfo, 0)[0]
  }

  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(
    programId: web3.PublicKey = new web3.PublicKey(
      'src5qyZHqTqecJV4aY6Cb6zDZLMDzrDKKezs22MPHr4'
    )
  ) {
    return beetSolana.GpaBuilder.fromStruct(
      programId,
      authorizedNativeSenderBeet
    )
  }

  /**
   * Deserializes the {@link AuthorizedNativeSender} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(
    buf: Buffer,
    offset = 0
  ): [AuthorizedNativeSender, number] {
    return authorizedNativeSenderBeet.deserialize(buf, offset)
  }

  /**
   * Serializes the {@link AuthorizedNativeSender} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return authorizedNativeSenderBeet.serialize({
      accountDiscriminator: authorizedNativeSenderDiscriminator,
      ...this,
    })
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link AuthorizedNativeSender} for the provided args.
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   */
  static byteSize(args: AuthorizedNativeSenderArgs) {
    const instance = AuthorizedNativeSender.fromArgs(args)
    return authorizedNativeSenderBeet.toFixedFromValue({
      accountDiscriminator: authorizedNativeSenderDiscriminator,
      ...instance,
    }).byteSize
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link AuthorizedNativeSender} data from rent
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    args: AuthorizedNativeSenderArgs,
    connection: web3.Connection,
    commitment?: web3.Commitment
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(
      AuthorizedNativeSender.byteSize(args),
      commitment
    )
  }

  /**
   * Returns a readable version of {@link AuthorizedNativeSender} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      dstAddress: this.dstAddress,
      bump: this.bump,
    }
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const authorizedNativeSenderBeet = new beet.FixableBeetStruct<
  AuthorizedNativeSender,
  AuthorizedNativeSenderArgs & {
    accountDiscriminator: number[] /* size: 8 */
  }
>(
  [
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['dstAddress', beet.bytes],
    ['bump', beet.u8],
  ],
  AuthorizedNativeSender.fromArgs,
  'AuthorizedNativeSender'
)
